spring:
    application:
        name: jobpulse
    datasource:
        url: jdbc:postgresql://localhost:5432/jobpulse
        username: postgres
        password: mysecretpassword
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
